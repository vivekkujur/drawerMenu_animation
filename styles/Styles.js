import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
    screenViewStyles: { backgroundColor: 'white', flex: 1, borderTopLeftRadius: 30, padding: 20, marginTop: 20 }
})